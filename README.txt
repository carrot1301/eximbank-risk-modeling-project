# 🏦 Eximbank Data Analysis Project

## 📄 Giới thiệu (Overview)
Dự án này tập trung vào việc phân tích dữ liệu khách hàng và hiệu quả của hoạt động Telesales tại Eximbank. Mục tiêu chính là tìm ra các insight về hành vi khách hàng, tỷ lệ chuyển đổi cuộc gọi và đề xuất các giải pháp tối ưu hóa quy trình chăm sóc khách hàng.

*(Bạn có thể sửa đoạn này để mô tả cụ thể hơn mục tiêu đồ án/bài tập của bạn)*

## 📂 Cấu trúc Dự án (Project Structure)

Dự án bao gồm các thành phần chính sau:

* **`project.ipynb`**: Source code chính (Jupyter Notebook) thực hiện các bước:
    * Làm sạch dữ liệu (Data Cleaning).
    * Phân tích thống kê mô tả (Descriptive Statistics).
    * Trực quan hóa dữ liệu (Data Visualization).
    * Xây dựng mô hình phân tích (nếu có).
* **`project.docx`**: Báo cáo chi tiết kết quả phân tích và các đề xuất kinh doanh.
* **`eximbank_customers_clean.csv`**: Dữ liệu khách hàng đã được làm sạch và chuẩn hóa.
* **`eximbank_telesales_merged_analysis.csv`**: Dữ liệu tổng hợp kết quả các cuộc gọi Telesales.

## 🛠 Công nghệ sử dụng (Technologies Used)
* **Ngôn ngữ:** Python 3.x
* **Thư viện:** Pandas, NumPy, Matplotlib/Seaborn (để vẽ biểu đồ), Scikit-learn (nếu có dùng mô hình).
* **Công cụ:** Jupyter Notebook / Google Colab.

## 🚀 Hướng dẫn chạy (How to Run)
1.  Clone dự án về máy:
    ```bash
    git clone [https://github.com/USERNAME-CUA-BAN/TEN-DU-AN.git](https://github.com/USERNAME-CUA-BAN/TEN-DU-AN.git)
    ```
2.  Cài đặt các thư viện cần thiết:
    ```bash
    pip install pandas numpy matplotlib seaborn
    ```
3.  Mở file `project.ipynb` bằng Jupyter Notebook hoặc VS Code để xem code và kết quả chạy.

## ⚠️ Disclaimer (Quan trọng)
Dự án này được thực hiện cho mục đích **học tập và nghiên cứu (Educational Purpose Only)**.

* Toàn bộ dữ liệu trong thư mục này là **dữ liệu giả lập (mock data)** hoặc đã được **ẩn danh hóa hoàn toàn (anonymized)**.
* Không có dữ liệu thực, thông tin cá nhân (PII) hoặc dữ liệu nhạy cảm nào của khách hàng thực tế được công khai trong kho lưu trữ này.
* Mọi sự trùng hợp về tên hoặc thông tin cá nhân (nếu có) chỉ là ngẫu nhiên.

---
**Author:**Doan Nguyen Tri
**Contact:** doantri12343@gmail.com